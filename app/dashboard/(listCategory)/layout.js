import React from "react";

const ListCategory = ({ children }) => {
  return (
    <React.Fragment>
      <div>ListCategory2</div>
      {children}
    </React.Fragment>
  );
};

export default ListCategory;
